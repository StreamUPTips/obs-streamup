---
phase: 03-warning-dialog-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/locale/en-US.ini
  - data/locale/en-GB.ini
  - data/locale/zh-CN.ini
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Dialog displays localized text to user"
  artifacts:
    - path: "data/locale/en-US.ini"
      provides: "Font.* localization keys"
      contains: "Font.Label.FontName"
    - path: "data/locale/en-GB.ini"
      provides: "Font.* localization keys"
      contains: "Font.Label.FontName"
    - path: "data/locale/zh-CN.ini"
      provides: "Font.* localization keys"
      contains: "Font.Label.FontName"
  key_links:
    - from: "core/file-manager.cpp"
      to: "data/locale/*.ini"
      via: "obs_module_text() calls"
      pattern: "obs_module_text\\(\"Font\\."
---

<objective>
Add missing Font.* localization keys to close the verification gap.

Purpose: The ShowMissingFontsDialog uses 7 Font.* localization keys via obs_module_text(), but these keys are not defined in the locale files. Without them, users see raw key names like "Font.Label.FontName" instead of "Font Name".

Output: All three locale files (en-US.ini, en-GB.ini, zh-CN.ini) will have the Font.* keys, enabling proper localized display in the missing fonts dialog.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-warning-dialog-ui/03-VERIFICATION.md
@data/locale/en-US.ini
@data/locale/en-GB.ini
@data/locale/zh-CN.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Font.* localization keys to all locale files</name>
  <files>
    data/locale/en-US.ini
    data/locale/en-GB.ini
    data/locale/zh-CN.ini
  </files>
  <action>
Add a new "FONT SYSTEM" section to each locale file after the PLUGIN MANAGEMENT section (around line 303 in en-US.ini).

**en-US.ini and en-GB.ini** (identical English strings):
```ini
# === FONT SYSTEM ===

Font.Label.FontName="Font Name"
Font.Label.DownloadLink="Download Link"
Font.Status.MissingFonts="Missing Fonts"
Font.Message.RequiredNotInstalled="The following fonts are required but not installed"
Font.Dialog.MissingGroup="Fonts Missing"
Font.Dialog.WarningContinue="Warning: By pressing continue anyway, the StreamUP file will load without these fonts. Text may appear incorrect until fonts are installed."
Font.Message.NoDownloadAvailable="No download available"
```

**zh-CN.ini** (Simplified Chinese translations):
```ini
# === FONT SYSTEM ===

Font.Label.FontName="字体名称"
Font.Label.DownloadLink="下载链接"
Font.Status.MissingFonts="缺失字体"
Font.Message.RequiredNotInstalled="以下字体为必需但未安装"
Font.Dialog.MissingGroup="缺失字体"
Font.Dialog.WarningContinue="警告：继续操作将加载不包含这些字体的 StreamUP 文件。在安装字体之前，文本可能显示不正确。"
Font.Message.NoDownloadAvailable="无可用下载"
```

Insert after the Plugin.Dialog.SkipTheseUpdates line and before the SETTINGS WINDOW section.
  </action>
  <verify>
    1. Run: `grep -n "Font\." data/locale/en-US.ini` - should show 7 Font.* keys
    2. Run: `grep -n "Font\." data/locale/en-GB.ini` - should show 7 Font.* keys
    3. Run: `grep -n "Font\." data/locale/zh-CN.ini` - should show 7 Font.* keys
    4. Verify each file has the new "FONT SYSTEM" section header
  </verify>
  <done>
    All 7 Font.* localization keys present in all 3 locale files:
    - Font.Label.FontName
    - Font.Label.DownloadLink
    - Font.Status.MissingFonts
    - Font.Message.RequiredNotInstalled
    - Font.Dialog.MissingGroup
    - Font.Dialog.WarningContinue
    - Font.Message.NoDownloadAvailable
  </done>
</task>

</tasks>

<verification>
1. Grep for Font.* keys in each locale file - all 7 should be present
2. Verify section placement is consistent across files
3. Build should complete without errors (no code changes, just locale files)
</verification>

<success_criteria>
- All 7 Font.* localization keys defined in en-US.ini, en-GB.ini, and zh-CN.ini
- Keys have appropriate translations for each locale
- obs_module_text("Font.*") calls will now return readable strings instead of key names
</success_criteria>

<output>
After completion, create `.planning/phases/03-warning-dialog-ui/03-02-SUMMARY.md`
</output>
